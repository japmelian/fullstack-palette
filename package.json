{
  "name": "fullstack-palette",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel --port 3000 src/index.html",
    "build": "parcel build src/index.html --public-url /fullstack-palette -d build",
    "deploy": "gh-pages -d build",
    "clean": "rm -rf package-lock.json dist .cache build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.10.0",
    "gh-pages": "^3.1.0",
    "parcel": "^1.12.4",
    "stylelint": "^13.7.2"
  },
  "dependencies": {
    "lodash": "^4.17.20"
  }
}
